apiVersion: config-manager.yuno.org/v1
kind: NodeInstance
metadata:
  labels:
    app.kubernetes.io/name: nodegroup
    app.kubernetes.io/managed-by: kustomize
  name: nodeinstance-sample
spec:
  # TODO(user): Add fields here
