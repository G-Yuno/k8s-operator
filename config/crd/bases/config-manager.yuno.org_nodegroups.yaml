---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nodegroups.config-manager.yuno.org
spec:
  group: config-manager.yuno.org
  names:
    kind: NodeGroup
    listKind: NodeGroupList
    plural: nodegroups
    shortNames:
    - np
    singular: nodegroup
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The version of Thanos Ruler
      jsonPath: .status.phase
      name: STATUS
      type: string
    - description: The number of instance
      jsonPath: .status.instanceNum
      name: instanceNum
      type: integer
    - description: The number of instance
      jsonPath: .status.syncSuccessNodeNum
      name: syncSuccessNodeNum
      type: integer
    - description: The number of instance
      jsonPath: .status.syncFailedNodeNum
      name: syncFailedNodeNum
      type: integer
    - description: The number of instance
      jsonPath: .status.pendingSyncNodeNum
      name: pendingSyncNodeNum
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: NodeGroup is the Schema for the nodegroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeGroupSpec defines the desired state of NodeGroup
            properties:
              fileManagers:
                items:
                  description: 配置文件
                  properties:
                    content:
                      type: string
                    mode:
                      type: string
                    path:
                      type: string
                  required:
                  - content
                  - mode
                  - path
                  type: object
                type: array
              ips:
                description: |-
                  Foo is an example field of NodeGroup. Edit nodegroup_types.go to remove/update
                  机器ip列表
                items:
                  type: string
                type: array
              kernelArgsManagers:
                items:
                  description: 内核参数
                  properties:
                    key:
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              packageManagers:
                items:
                  description: 软件包管理
                  properties:
                    name:
                      type: string
                    version:
                      type: string
                  required:
                  - name
                  - version
                  type: object
                type: array
              postRestartServices:
                items:
                  type: string
                type: array
            required:
            - ips
            type: object
          status:
            description: NodeGroupStatus defines the observed state of NodeGroup.
            properties:
              ansiblePlayBookYamlString:
                type: string
              conditions:
                description: |-
                  conditions represent the current state of the NodeGroup resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              instanceNum:
                type: integer
              lastContentHash:
                description: 上一次和这一次模板的hash值
                type: string
              pendingSyncNodeNum:
                type: integer
              phase:
                type: string
              syncFailedNodeNum:
                type: integer
              syncSuccessNodeNum:
                description: 作为一个池子得知道当前有多少个节点已同步，多少个节点同步失败
                type: integer
              thisContentHash:
                type: string
            required:
            - ansiblePlayBookYamlString
            - instanceNum
            - lastContentHash
            - pendingSyncNodeNum
            - syncFailedNodeNum
            - syncSuccessNodeNum
            - thisContentHash
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
